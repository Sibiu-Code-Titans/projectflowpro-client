export type TaskDetailsSidebarAssigneeModel = {
  id: string;
  image: string;
  isSelected: boolean;
  name: string;
};

export type TaskDetailsSidebarPersonModel = {
  id: string;
  image: string;
  name: string;
};
